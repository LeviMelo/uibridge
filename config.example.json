// Copy to config.json in your uibridge home (`uibridge paths`) to override
// defaults. Every key here is optional; the shipped defaults are measured.
{
  "port": 8477,
  "host": "127.0.0.1",
  "defaultProvider": "gemini",
  "headless": "offscreen",
  "profileDir": ".profiles",
  "downloadDir": "downloads",
  "provider": {
    "concurrency": 2,
    "responseTimeoutMs": 600000,
    "pollMs": 150
  },
  "providers": {
    "gemini": {
      "headless": true
    },
    "chatgpt": {
      "headless": "offscreen"
    }
  }
}
