[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "uibridge"
version = "0.4.0"
description = "Use your own Gemini and ChatGPT subscriptions from Python, through the local uibridge daemon."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["uibridge"]
